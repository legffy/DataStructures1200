lines I used to compile and run the code:
 g++ -o road.exe road.cpp line.cpp point.cpp
 .\road.exe input_a.txt
 doesn't run because it never compiled correctly
 the line.h and line.cpp file both included the point.h file when only one of them neede it
 roads.cpp didn't have #include algorithm

 